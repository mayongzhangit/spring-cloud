spring:
  profiles:
    active: local
    include: tomcat,feign,sentinel,hystrix,ribbon,zipkin,eureka
  mvc:
    servlet:
      loadOnStartup: 1
management:
  endpoints:
    web:
      basePath: /actuator
      exposure:
        include: "*"
my:
  data: myz
---
spring:
  profiles: local

---
spring:
  profiles: dev

---
spring:
  profiles: prod