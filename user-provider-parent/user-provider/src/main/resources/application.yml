spring:
  profiles:
    active: local
    include: tomcat,feign,sentinel,hystrix,ribbon,zipkin,sba,nacos #,eureka
  mvc:
    servlet:
      loadOnStartup: 1

---
spring:
  profiles: local

---
spring:
  profiles: dev

---
spring:
  profiles: prod